import styles from "./NavBar.module.css";
import { useNavigate } from "react-router-dom";

function NavBar({
}) {

    return (
        <div>
          
        </div>
    );
}

export default NavBar;